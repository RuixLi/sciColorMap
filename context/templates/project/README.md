# <package name>

> The human face, lead-first, for someone deciding whether to use it. Realizes the README pattern (`where-files-live`). Replace every `<placeholder>`, delete these `>` notes.
> Badges optional: version, license, language.
> Study variant: a study README runs thinner - purpose, how to run, and a pointer to `AGENTS.md` and `context/MAP.md`; drop Features / Installation / Quick start / Usage.

<one sentence: what this is and for whom>.

## Features

> A short bullet list of what it does. The few that matter, not all.

- <capability>
- <capability>

## Installation

> Exact steps; pin the environment. A reader should clone and run these verbatim.

```
<e.g. pip install -e .   |   git clone <url> && cd <pkg>>
```

## Quick start

> The smallest runnable example - the one that proves it works.

```<lang>
<minimal example>
```

## Usage

> A fuller example; point to the API gateway (`__init__.py` / `Contents.m`) for the full surface.

```<lang>
<fuller example>
```

## License / references

> License line; citation if academic.

- License: <SPDX id, e.g. MIT> - see `LICENSE`.
- <citation / references, if any>.
