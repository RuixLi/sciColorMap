# Environment

> The pinned environment to run this repo, language-agnostic. Use this form when there is no single-language manifest - a polyglot study, a markdown / MATLAB package, mixed tooling. A Python package uses `pyproject.toml` instead; conda uses `environment.yml`. Replace every `<placeholder>`, delete these `>` notes.

## Toolchain

> Every tool a fresh machine needs, pinned. A reader should reproduce the environment from this list alone.

- <tool> <version> - <why it is needed>.
- e.g. Python 3.11; MATLAB R2023b; Node 20.

## Dependencies

> Per language / tool, the pinned packages. Point to the lockfile where one exists.

- Python: see `pyproject.toml` / `requirements.txt` (pinned).
- MATLAB: <toolboxes + versions>.
- <other>: <...>.

## Setup

> The exact steps from clone to runnable. Verbatim and reproducible.

1. <step>.
2. <step>.

## Notes

> Anything environment-specific that bites: OS assumptions, absolute paths, secrets handling.

- <note>.
